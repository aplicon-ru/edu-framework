# CHANGELOG · D11 Коммуникации и публичность

## [Не выпущено] — 2026-07-15

### Добавлено
- `site-requirements-registry.md` — реестр 16 обязательных информационных блоков
  сайта вуза (`OO-01` — `AT-01`), не только `/sveden/`: противодействие коррупции,
  финансовая открытость, политика ПДн, СОУТ, вакансии/квота, диссоветы, приёмная
  кампания, целевое обучение, ЭИОС, доступность (2 блока), инфобезопасность,
  антитеррор (открытый слой, двухслойный контур ДСП/публичное). Источник —
  внешний черновик пользователя (v0.3), интегрирован с точечной сверкой номера
  приказа для `OO-01` (актуализирован до № 1493 в ред. № 1353, использован тот же,
  что уже проверен в `standards/sveden/README.md`) и добавлением перекрёстных
  ссылок на уже собранный подреестр `standards/sveden/`.
- README.md/GUIDE.md: упоминание нового реестра, явное разграничение —
  `OO-01`/`sveden` = рабочий процесс D11-P01, остальные 15 блоков = каталог
  обязательств, процессы для них не написаны.

### Изначальный драфт
Реестр — внешний черновик пользователя, нормативка в нём требует дальнейшей
сверки (см. §7 «Открытые вопросы» внутри файла, перенесены как есть, кроме
пункта про `OO-01`, решённого при интеграции). Интеграция и точечная сверка:
Claude Sonnet 5.

### Открытые вопросы (перенесены из источника + новые при интеграции)
- Дата переноса `accreditationDocLink` в подраздел `education` — в
  `site-requirements-registry.md` указано 01.03.2026, в `vocab/base.yaml` —
  01.09.2025. Расхождение не устранено, сверить оба источника.
- Блоки `GU-04`, `NO-01`, `RD-01`/`RD-02` содержательно принадлежат другим
  доменам (D09, D08, D07) — атрибут `data_source_domain` в §6 реестра не
  проставлен, кандидат на следующую правку.
- Остальные пункты §7 источника (реквизиты Порядка приёма, целевого обучения,
  530н, комплексного плана ПИТ, кандидаты ГО/ЧС и пожарной безопасности) —
  без изменений, требуют сверки человеком.

## [0.1.0] — 2026-06-11

### Добавлено
- Структура домена: README, GUIDE, metadata.yml, processes/, data-model/
- D11-P01: ведение раздела «Сведения об образовательной организации» (`/sveden/`) —
  определение применимости подразделов, сборка канонической модели данных,
  публикация и микроразметка, самопроверка, реакция на новые редакции приказа
- data-model/entities.md: модель проекции (СведенияРаздел, ПолеРазметки,
  ГруппаРазметки, ЭлектронныйДокумент, ЧекСоответствия) + 5 инвариантов
- GUIDE.md: карта источников данных по подразделам, правило «применим/неприменим»

### Архитектурное решение
D11 — новый домен, выделен под обязательство ст. 29 ФЗ-273 (информационная
открытость), ранее не покрытое ни одним доменом. D11 не вводит собственную модель
данных — проецирует каноническую модель организации
([`standards/sveden/schema/instance.schema.yaml`](../../standards/sveden/schema/instance.schema.yaml))
в структуру `/sveden/` по словарю
[`standards/sveden/vocab/base.yaml`](../../standards/sveden/vocab/base.yaml).

### Источники и авторство
- Структура раздела `/sveden/`, словарь микроразметки (v9.0.0) и каноническая схема
  данных — из рабочих каркасов лицензирования ДО/ДПО (handoff, iMironRU, 2026),
  вынесены в `standards/sveden/`.
- Структурирование домена D11: Claude Sonnet 4.6.

### Открытые вопросы
- Часть полей словаря остаётся `from: "TBD"` — см.
  `standards/sveden/schema/instance.schema.suggestions.yaml`.
- Требование `a11y: copy` — требует эмпирической проверки актуальности в АИС
  «Мониторинг» (см. `standards/sveden/CHANGELOG.md`).
- Граница D11 / D12 по сайту целиком (вне `/sveden/`) — уточнить при заполнении D12.
